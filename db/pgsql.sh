mkdir -p $PGSQL_PATH
docker run --rm --name $PGSQL_CONTAINER_NAME -p $PGSQL_PORT:$PGSQL_PORT -e POSTGRES_USER=$DATABASE_USERNAME -e POSTGRES_PASSWORD=$DATABASE_PASSWORD -e POSTGRES_DB=$DATABASE_NAME -v $PGSQL_PATH:/var/lib/postgresql/data postgres:latest
